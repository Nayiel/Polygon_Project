#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "scan_line.h"
#include "jsmn.h"
el[20000];
int len = fread(bel, 1, sizeof(bel), file); 
fclose(fp);

jsmn_parser p ;

jsmn_init(&p);
jmsn_parse(&p,file,)
FILE *file = fopen("geoBondaries-BEL-ADM0.geojson","r");
char b



